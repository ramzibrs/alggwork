### How To Open :

**Note:** 
mysql port 3307
nginx port 8080,443
php-fpm port 9000

After cloning Repository
```bach 
sudo docker login
```
after that  we run 
```bach 
sudo docker-compose build 
```
finally we run 
```bach 
sudo docker-compose up -d
```